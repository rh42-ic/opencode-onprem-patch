diff --git a/packages/opencode/src/file/ripgrep.ts b/packages/opencode/src/file/ripgrep.ts
index d6fd61f..9ede2e1 100644
--- a/packages/opencode/src/file/ripgrep.ts
+++ b/packages/opencode/src/file/ripgrep.ts
@@ -10,6 +10,7 @@ import { ChildProcessSpawner } from "effect/unstable/process/ChildProcessSpawner
 import * as CrossSpawnSpawner from "@/effect/cross-spawn-spawner"
 import { Global } from "@/global"
 import { Log } from "@/util"
+import { Onprem } from "../onprem"
 import { sanitizedProcessEnv } from "@/util/opencode-process"
 import { which } from "@/util/which"
 
@@ -291,6 +292,16 @@ export const layer: Layer.Layer<Service, never, AppFileSystem.Service | ChildPro
 
       const filepath = yield* Effect.cached(
         Effect.gen(function* () {
+          // onprem-fork: check offline deps for bundled ripgrep binary
+          if (Onprem.isEnabled()) {
+            const offlinePath = Onprem.resolveBinary("rg" + (process.platform === "win32" ? ".exe" : ""), "ripgrep")
+            if (offlinePath && (yield* fs.isFile(offlinePath).pipe(Effect.orDie))) {
+              log.info("using onprem ripgrep", { path: offlinePath })
+              return offlinePath
+            }
+            log.warn("onprem mode enabled but ripgrep not found in deps")
+          }
+
           const system = yield* Effect.sync(() => which(process.platform === "win32" ? "rg.exe" : "rg"))
           if (system && (yield* fs.isFile(system).pipe(Effect.orDie))) return system
 
diff --git a/packages/opencode/src/flag/flag.ts b/packages/opencode/src/flag/flag.ts
index 72c8931..abcedc6 100644
--- a/packages/opencode/src/flag/flag.ts
+++ b/packages/opencode/src/flag/flag.ts
@@ -80,6 +80,10 @@ export const Flag = {
   OPENCODE_SKIP_MIGRATIONS: truthy("OPENCODE_SKIP_MIGRATIONS"),
   OPENCODE_STRICT_CONFIG_DEPS: truthy("OPENCODE_STRICT_CONFIG_DEPS"),
 
+  // onprem-fork: onprem mode flags
+  OPENCODE_ONPREM_MODE: truthy("OPENCODE_ONPREM_MODE"),
+  OPENCODE_ONPREM_DEPS_PATH: process.env["OPENCODE_ONPREM_DEPS_PATH"],
+
   OPENCODE_WORKSPACE_ID: process.env["OPENCODE_WORKSPACE_ID"],
   OPENCODE_EXPERIMENTAL_HTTPAPI: truthy("OPENCODE_EXPERIMENTAL_HTTPAPI"),
   OPENCODE_EXPERIMENTAL_WORKSPACES: OPENCODE_EXPERIMENTAL || truthy("OPENCODE_EXPERIMENTAL_WORKSPACES"),
diff --git a/packages/opencode/src/provider/models.ts b/packages/opencode/src/provider/models.ts
index 2924666..febf5b2 100644
--- a/packages/opencode/src/provider/models.ts
+++ b/packages/opencode/src/provider/models.ts
@@ -8,6 +8,7 @@ import { lazy } from "@/util/lazy"
 import { Filesystem } from "../util"
 import { Flock } from "@opencode-ai/shared/util/flock"
 import { Hash } from "@opencode-ai/shared/util/hash"
+import { Onprem } from "../onprem"
 
 // Try to import bundled snapshot (generated at build time)
 // Falls back to undefined in dev mode when snapshot doesn't exist
@@ -130,6 +131,18 @@ const fetchApi = async () => {
 export const Data = lazy(async () => {
   const result = await Filesystem.readJson(Flag.OPENCODE_MODELS_PATH ?? filepath).catch(() => {})
   if (result) return result
+  // onprem-fork: try deps models.json from onprem bundle before snapshot
+  if (Onprem.isEnabled()) {
+    const depsPath = Onprem.getDepsPath()
+    if (depsPath) {
+      const offlineResult = await Filesystem.readJson(path.join(depsPath, "models.json")).catch(() => {})
+      if (offlineResult) {
+        log.info("loaded models from onprem deps", { path: depsPath })
+        return offlineResult as Record<string, Provider>
+      }
+      log.warn("onprem enabled but deps models.json not found", { path: depsPath })
+    }
+  }
   // @ts-ignore
   const snapshot = await import("./models-snapshot.js")
     .then((m) => m.snapshot as Record<string, unknown>)
@@ -150,6 +163,19 @@ export const Data = lazy(async () => {
 })
 
 export async function get() {
+  // onprem-fork: in onprem mode, try bundled models from deps first
+  if (Onprem.isEnabled()) {
+    const depsPath = Onprem.getDepsPath()
+    if (depsPath) {
+      const modelsPath = path.join(depsPath, "models.json")
+      const offlineResult = await Filesystem.readJson(modelsPath).catch((e) => {
+        log.warn("failed to read onprem models.json", { path: modelsPath, error: String(e) })
+        return undefined
+      })
+      if (offlineResult) return offlineResult as Record<string, Provider>
+    }
+  }
+
   const result = await Data()
   return result as Record<string, Provider>
 }
@@ -169,7 +195,8 @@ export async function refresh(force = false) {
   })
 }
 
-if (!Flag.OPENCODE_DISABLE_MODELS_FETCH && !process.argv.includes("--get-yargs-completions")) {
+// onprem-fork: skip models refresh in onprem mode
+if (!Onprem.isEnabled() && !Flag.OPENCODE_DISABLE_MODELS_FETCH && !process.argv.includes("--get-yargs-completions")) {
   void refresh()
   setInterval(
     async () => {
diff --git a/packages/opencode/src/server/routes/ui.ts b/packages/opencode/src/server/routes/ui.ts
index d449cd1..3fb8051 100644
--- a/packages/opencode/src/server/routes/ui.ts
+++ b/packages/opencode/src/server/routes/ui.ts
@@ -4,6 +4,7 @@ import { proxy } from "hono/proxy"
 import { getMimeType } from "hono/utils/mime"
 import { createHash } from "node:crypto"
 import fs from "node:fs/promises"
+import { Onprem } from "../../onprem"
 
 const embeddedUIPromise = Flag.OPENCODE_DISABLE_EMBEDDED_WEB_UI
   ? Promise.resolve(null)
@@ -18,6 +19,12 @@ const csp = (hash = "") =>
 
 export const UIRoutes = (): Hono =>
   new Hono().all("/*", async (c) => {
+    // onprem-fork: serve bundled web app in onprem mode
+    const onpremStatic = await Onprem.tryServeStaticFile(c.req.path)
+    if (onpremStatic) {
+      return new Response(onpremStatic.body, { headers: { "Content-Type": onpremStatic.mime } })
+    }
+
     const embeddedWebUI = await embeddedUIPromise
     const path = c.req.path
 
